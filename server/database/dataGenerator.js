const faker = require('faker');

const db = require('./dbConnection.js');

const photoGenerator = () => {
  const queryString =
  const photoRecord = [
    `${faker.image.imageUrl}`,
  ];
  db.query()
};